import {jest} from '@jest/globals';

export const deploy = jest.fn().mockImplementation(async () => {});

export const deployOneGuild = jest.fn().mockImplementation(async () => {});

export const initializeCommand = jest.fn().mockImplementation(async () => {});